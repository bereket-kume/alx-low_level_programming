#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *puts2 - is function that prints number
 *@str: is parameter that given to our function
 *Retrun: nothing
 */
void puts2(char *str)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{
		if (a % 2 == 0)
	{
		_putchar(s[a]);
	}
	}
	_putchar('\n');i
}

