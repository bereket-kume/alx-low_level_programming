#include "main.h"
#include <string.h>
/**
 *_strcpy - is the function that copy text
 *@dest: is our first parameter
 *@src: is our second parameter
 *Return: nothing
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest,src);
	return (dest);
}
