#include "main.h"
#include <string.h>
#include <stdio.h>
#include <stdbool.h>
/**
 *is_palindrome - is function that display string
 *@s: is our first parameter
 *Return: 1 or 0
 */
int is_palindrome(char *s)
{
	int length = strlen(s);
	if (length == 0)
	{
		return 1;
	}
}
