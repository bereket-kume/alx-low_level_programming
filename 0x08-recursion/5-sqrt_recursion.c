#include "main.h"
/**
 *_sqrt_recursion - is function that give square
 *@n: is our first parameter
 *Return: alwayas n
 */
int _sqrt_recursion(int n)
{
	if (n == 1 || n == 0)
	{
		return (n);
	}
	return (1);
}
