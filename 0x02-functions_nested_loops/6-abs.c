#include "main.h"
/**
 *_abs - give us absoulte value of number
 *@int value passed as parameter
 *Return: Always 0
 */
int _abs(int)
{
	if (i > 0)
		return (i);
	else if (i < 0)
		return (-i);
	else
		return (0);
}
