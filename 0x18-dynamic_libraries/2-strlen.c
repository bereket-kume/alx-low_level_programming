#include "main.h"
#include <string.h>
/**
 *_strlen - is function that display length of string
 *@s: is parameter of our function
 *Return: lenght of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
